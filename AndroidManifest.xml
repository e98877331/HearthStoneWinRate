<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wcm.towolf.hearthstonewr"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Black.NoTitleBar"
        android:name="MyApplication" >
        <activity
            android:name="wcm.towolf.hearthstonewr.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="WelcomeActivity">
        </activity>
        <activity android:name="DetailActivity">
        </activity>
        <activity
            android:name="wcm.towolf.heartstonewr.detail.DialogActivity"
            android:theme="@android:style/Theme.Dialog"
            android:label="@string/detail_dialog_label" >
        </activity>
        <activity android:name="wcm.towolf.heartstonewr.detail.DetailListActivity"
            android:theme= "@android:style/Theme.Light.NoTitleBar"></activity>

    </application>

</manifest>